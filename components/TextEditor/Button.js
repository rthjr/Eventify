import styled from "@emotion/styled";

export const Button = styled.button`
  border: none;
  background: none;
  cursor: pointer;
  margin: 0 5px;
  font-size: 18px;

  &:hover {
    background: #ddd;
  }
`;