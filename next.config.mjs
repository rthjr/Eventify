/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.imghippo.com",
      "images.unsplash.com",
      "res.cloudinary.com"
    ],
  },
};

export default nextConfig;
